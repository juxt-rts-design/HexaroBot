{
  "name": "hexaro",
  "version": "1.0.0",
  "private": true,
  "description": "HEXARO — backend + frontend",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "concurrently -n backend,frontend -c green,cyan \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "start:prod": "npm run build && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
